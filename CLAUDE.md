# Instruktioner til Claude

- **Selvtjek altid før en task rapporteres som færdig.** Kør koden, verificér
  beregninger manuelt (ikke kun "det kører uden fejl"), og test UI-flows i en
  rigtig browser (fx via Playwright), når det er relevant. Sig eksplicit hvis
  noget ikke er blevet testet, i stedet for at antage det virker.
